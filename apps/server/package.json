{
  "name": "@baro-ddulrim/server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "prisma generate && nest start --watch",
    "dev2": "cp .env.dev .env && dotenv -e .env -- nest start --watch",
    "prd2": "cp .env.prd .env && dotenv -e .env -- node dist/main.js",
    "build": "prisma generate && nest build",
    "start": "node dist/main.js",
    "lint": "tsc --noEmit",
    "typecheck": "prisma generate && tsc --noEmit",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:migrate:dev": "cp .env.dev .env && dotenv -e .env -- prisma migrate deploy",
    "db:migrate:prd": "cp .env.prd .env && dotenv -e .env -- prisma migrate deploy",
    "db:push": "prisma db push",
    "db:seed": "prisma generate && prisma db seed"
  },
  "dependencies": {
    "@nestjs/common": "latest",
    "@nestjs/config": "latest",
    "@nestjs/core": "latest",
    "@nestjs/platform-express": "latest",
    "@nestjs/schedule": "latest",
    "@nestjs/swagger": "latest",
    "@prisma/adapter-pg": "latest",
    "@prisma/client": "latest",
    "@supabase/supabase-js": "latest",
    "class-transformer": "latest",
    "class-validator": "latest",
    "dotenv": "latest",
    "firebase-admin": "latest",
    "jose": "latest",
    "nodemailer": "latest",
    "pg": "latest",
    "reflect-metadata": "latest",
    "rxjs": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@nestjs/cli": "latest",
    "@nestjs/schematics": "latest",
    "@nestjs/testing": "latest",
    "@types/express": "latest",
    "@types/node": "latest",
    "@types/nodemailer": "latest",
    "@types/pg": "latest",
    "prisma": "latest",
    "ts-node": "latest",
    "tsx": "latest",
    "typescript": "^5.9.0"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
